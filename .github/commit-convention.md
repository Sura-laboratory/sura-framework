# Шаблон коммита

Пожалуйста, придерживайтесь следующего формата при написании сообщений коммитов:
<тип>(<область>): <описание>

[дополнительное описание]

Closes: #<номер-задачи>


## Типы коммитов

- `feat` - Добавление нового функционала
- `fix` - Исправление ошибок
- `docs` - Изменения в документации
- `style` - Изменения, не влияющие на код (форматирование, точки с запятой и т.д.)
- `refactor` - Переработка кода без изменения поведения
- `perf` - Оптимизация производительности
- `test` - Добавление или исправление тестов
- `chore` - Обновление билд-системы или вспомогательных инструментов
- `build` - Изменения, связанные со сборкой или зависимостями
- `ci` - Изменения в CI/CD конфигурации

> 💡 Эти типы соответствуют стандарту [Conventional Commits](https://www.conventionalcommits.org/).

---

## Семантическое версионирование

Наш проект использует автоматическое управление версиями через `semantic-release`.  
Номер версии увеличивается в зависимости от типа коммита:

| Тип коммита | Пример | Влияние на версию |
|-------------|-------|-------------------|
| `feat` | `feat(auth): добавить двухфакторную аутентификацию` | **Minor** → `1.2.0` → `1.3.0` |
| `fix`, `perf`, `build` | `fix(api): исправить утечку памяти` | **Patch** → `1.2.0` → `1.2.1` |
| Любой коммит с `!` или `BREAKING CHANGE` | `feat!: удалить поддержку старого API`<br>или<br>`feat(auth): переписать систему входа`<br><br>`BREAKING CHANGE: изменён формат токена` | **Major** → `1.2.0` → `2.0.0` |

> 🔺 **Major-версия** повышается, если:
> - В заголовке коммита есть восклицательный знак: `feat!:` или `fix!:` и т.д.
> - Или в теле коммита указано `BREAKING CHANGE: ...`

> ❌ Коммиты с типами `docs`, `style`, `refactor`, `test`, `chore`, `ci` — **не создают новый релиз**.

---

## Примеры

### Новая фича → minor-версия
feat(auth): добавить проверку пароля

Реализована проверка сложности пароля при регистрации пользователя.
Минимальная длина — 8 символов, требуется наличие цифр и букв.

Closes: #123


### Исправление бага → patch-версия
fix(api): исправить обработку ошибок 500

Обработка внутренних ошибок сервера теперь корректно возвращает JSON.
Ранее возвращался plain text, что вызывало проблемы у клиентов.

Closes: #156


### Критическое изменение → major-версия
feat!: удалить поддержку Node.js 14

BREAKING CHANGE: проект теперь требует Node.js 16 или выше.
Обновите окружение перед обновлением пакета.

Closes: #189


---

Соблюдение этого формата помогает автоматически генерировать:
- Номер версии
- Release Notes
- Историю изменений

Спасибо за чистую историю коммитов! 🙌